import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webscraper.pro',
  appName: 'WebScraperPro',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
